# secrets — never commit
.env

# python venv
bin/
lib/
lib64
include/
pyvenv.cfg
__pycache__/
*.pyc

# local runtime data (copies of ERP data, user alert prefs)
cache/
alerts_state.json
app_config.json
learned_aliases.json
recordings/
voice_attempts.jsonl

# test artifacts
login_result.png

# TLS (mkcert) private keys — never commit
certs/

# company website lives on the "www" branch; local untracked copy is the
# live deployment (symlinked from /srv/www/lucia)
site/
voice_log.jsonl
voice_customer_eval/
voice_map_experiment/
voice_recent_random_test/
customer_voice_focus.json
customer_spoken_names.txt

# SenseVoice ASR experiment model (229M, downloaded on demand)
models/

# runtime error log for failed ERP writes
api_errors.jsonl
